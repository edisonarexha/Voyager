// Imports: local files.
const statusCodes = require("./statusCodes");

// Bundler object that is used to export all config objects in this folder.
const bundler = { statusCodes };

// Exports of this file.
module.exports = bundler;
